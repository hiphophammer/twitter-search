import Button from 'react-bootstrap/Button';

const ConfirmButton = ( ) => {
  return (
    <>
      <Button className='confirmButton' variant="dark" active={ true }>확인</Button>{' '}
    </>
  )
};

export default ConfirmButton;